@model ShopProject.ViewModel.AddWatchVM

@{
    ViewBag.Title = "AddWatch";
}

<h2>Dodawanie Zegarka</h2>

@using (Html.BeginForm("AddWatch", "Watch", FormMethod.Post, new { enctype = "multipart/form-data" }))
{
<table>
    <tr>
        <td>@Html.DisplayNameFor(model => Model.watch.Name)</td>
        <td>@Html.EditorFor(model => Model.watch.Name)</td>
    </tr>
    <tr>
        <td>@Html.DisplayNameFor(model => Model.watch.Cost)</td>
        <td>@Html.EditorFor(model => Model.watch.Cost)</td>
    </tr>
    <tr>
        <td>@Html.DisplayNameFor(model => Model.watch.Description)</td>
        <td>@Html.EditorFor(model => Model.watch.Description)</td>
    </tr>

    <tr>
        <td>
            <input type="file" name="imageFile" size="23" accept="gif|jpg|png" />
            @Html.ValidationMessageFor(model => model.watch.ProductImage)
        </td>
    </tr>
</table>


    <div class="form-horizontal">

        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </div>
    </div>
}